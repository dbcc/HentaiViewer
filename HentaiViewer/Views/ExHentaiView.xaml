<UserControl x:Class="HentaiViewer.Views.ExHentaiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HentaiViewer.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:models="clr-namespace:HentaiViewer.Models"
             xmlns:viewModel="clr-namespace:HentaiViewer.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance {x:Type viewModel:ExHentaiViewModel}, IsDesignTimeCreatable=False}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<Button Background="Transparent" HorizontalAlignment="Center" Height="50" Width="50" Padding="2" Margin="5,0"
			        Command="{Binding RefreshExHentaiCommand}"
			        Grid.RowSpan="2">
				<materialDesign:PackIcon Kind="Refresh" Width="35" Height="35" />
			</Button>
			<Label Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"
			       Content="{Binding Value, ElementName=Slider}" Margin="5,0" />
			<Slider Grid.Column="2" x:Name="Slider" Minimum="150" Maximum="850" VerticalAlignment="Center" Margin="5,0,10,0"
			        Value="170" SmallChange="1"
			        IsSnapToTickEnabled="True" />

			<Grid Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Button Background="Transparent" Height="25" Padding="2" Margin="5,0" Command="{Binding LoadPrevExHentaiCommand}">
					<materialDesign:PackIcon Kind="ArrowLeft" />
				</Button>
				<TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0">
					<Run Text="Page " />
					<Run Text="{Binding ExHentaiLoadedPage}" />
				</TextBlock>
				<Button Grid.Column="2" Background="Transparent" Height="25" Padding="2" Margin="5,0"
				        Command="{Binding LoadMoreExHentaiCommand}">
					<materialDesign:PackIcon Kind="ArrowRight" />
				</Button>
			</Grid>
		</Grid>


		<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<ItemsControl Grid.Row="0" ItemsSource="{Binding ExHentaiItems}">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel />
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<models:HentaiCard Width="{Binding Value, ElementName=Slider}" />
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
				<Button Grid.Row="1" Background="Transparent" Margin="10,5" Command="{Binding LoadMoreExHentaiCommand}">
					<TextBlock>
						<Run Text="Load Page " />
						<Run Text="{Binding NextExHentaiPage}" />
					</TextBlock>
				</Button>
			</Grid>
		</ScrollViewer>
	</Grid>
</UserControl>